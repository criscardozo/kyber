{
  "name": "kyber",
  "private": true,
  "type": "module",
  "description": "What the household apps share: scripts that read the consumer's .kyber/config.json, the working rules that travel, and the declared stack.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test && python3 -m unittest discover -q -s test -p 'test_*.py' > /dev/null",
    "check": "for f in scripts/*.mjs scripts/lib/*.mjs test/*.mjs design/*.mjs; do node --check \"$f\" || exit 1; done && python3 -m compileall -q design test"
  },
  "peerDependencies": {
    "firebase-admin": ">=14",
    "firebase-tools": ">=15",
    "vitest": ">=4"
  },
  "peerDependenciesMeta": {
    "firebase-admin": { "optional": true },
    "firebase-tools": { "optional": true },
    "vitest": { "optional": true }
  }
}
